<div class="col-md-6 col-md-offset-3">
	  <div class="page-header text-contrast">
	    <h1>New Shop</h1>
	  </div>

	  <div ng-show="error" class="alert alert-danger row">
	    <span>{{ error.message }}</span>
	  </div>

	  <form ng-submit="create()"
	    style="margin-top:30px;">
		
	    <div class="form-group">
	      <input type="text"
	      class="form-control"
	      placeholder="Business"
	      ng-model="shop.business"></input>
	    </div>
		<div class="form-group">
	      <select class="form-control" id="type"
	      ng-model="shop.type" ng-options="type for type in types">
	      	<option value="" selected>選擇行業</option>
	      </select>
	    </div>
	    <div class="form-group">
	      <select class="form-control" id="region"
	      ng-model="shop.region" ng-options="c.name as c.name for c in regions">
	      	<option value="" selected>選擇地區</option>
	      </select>
	    </div>
	    <div class="form-group">
	      <input type="text"
	      class="form-control"
	      placeholder="Serial Number"
	      ng-model="shop.serialNumber"></input>
	    </div>
	    <div><button type="submit" class="btn btn-primary">Create</button></div>
	  </form>
</div>